[alias]
check-all = "check --all-targets --all-features"
doc-check = "rustdoc --all-features -- -D warnings"
fmt-check = "fmt --all -- --check"
lint = "clippy --all-targets --all-features -- -D warnings"
test-all = "test --all-targets --all-features"
